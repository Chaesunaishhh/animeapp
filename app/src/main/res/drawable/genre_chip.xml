<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    android:shape="rectangle">

    <!-- Rounded corners -->
    <corners android:radius="16dp"/>

    <!-- Background color -->
    <solid android:color="@color/colorPink"/>

    <!-- Padding sa loob ng chip -->
    <padding
        android:left="12dp"
        android:top="6dp"
        android:right="12dp"
        android:bottom="6dp"/>
</shape>